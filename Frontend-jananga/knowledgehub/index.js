function createCard() {
    var parent = document.getElementById("card_parent");
    let card = document.createElement("div");
    card.className = "card";

    parent.appendChild(card);
}

document.addEventListener("DOMContentLoaded", function(){
});